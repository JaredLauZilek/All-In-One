# Copy to .env and fill in. Everything here ships in the browser bundle (VITE_ prefix),
# so only ever put PUBLIC values here — never the service-role key.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_KEY=sb_publishable_...
VITE_SUPABASE_ANON_KEY=legacy_public_anon_jwt
